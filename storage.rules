rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 임시 파일 업로드 허용 (10MB 제한) - 모든 경로 패턴 허용
    match /temp/{allPaths=**} {
      allow read, write, delete: if request.resource == null || request.resource.size < 10 * 1024 * 1024;
    }
  }
}
